let seatPriceMap = [];
for(let i=1;i<15;i++){
    seatPriceMap[`A${i}`] = 100000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`B${i}`] = 100000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`C${i}`] = 120000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`D${i}`] = 120000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`E${i}`] = 120000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`F${i}`] = 120000;
}
for(let i=1;i<15;i++){
    seatPriceMap[`G${i}`] = 180000;
}
export default seatPriceMap;